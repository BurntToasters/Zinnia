<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- allow-jit: required for JavaScript JIT compilation in WKWebView -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- allow-unsigned-executable-memory: required alongside allow-jit for
         certain WKWebView memory mapping operations on some macOS versions -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- disable-library-validation: required for WKWebView to load its
         dynamic libraries (WebKit frameworks) at runtime -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- network.client: required for the updater to reach GitHub releases -->
    <key>com.apple.security.network.client</key>
    <true/>
  </dict>
</plist>
